/**
 * Platform bootstrap-layer SDK — shared runnable Spring Boot infrastructure.
 *
 * <p>Provides logback profiles, actuator defaults, and graceful shutdown via auto-configuration.
 * Consumers add {@code implementation project(':platform-bootstrap')} (or published artifact) only.
 */
package com.ryuqqq.platform.bootstrap;
