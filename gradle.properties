# 빌드 캐시 — 안 바뀐 태스크 출력을 재사용. CI는 gradle/actions/setup-gradle가 런 간 persist.
org.gradle.caching=true

# 모듈 병렬 빌드 — 모듈이 독립적이라 안전. 멀티모듈 빌드 가속.
org.gradle.parallel=true

# 데몬 힙·메타스페이스 명시 — 병렬 빌드 시 CI(제한 메모리)의 OOM(exit 137) 방지.
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
