package com.ryuqqq.platform.archrules.fixture.domainconv.violation.svc.domain.query;

/** Violation SortKey — enum이 아닌 class(R11). */
public class BadSortKey {
    public String fieldName() {
        return "createdAt";
    }
}
